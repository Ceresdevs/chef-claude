export default function Main() {
    return (
        <main>
            <form className="ingredient-form">
                <input type="text" aria-label="Add ingredient" placeholder="e.g. Chicken" />
                <button>Add ingredient</button>
            </form>
        </main> 
    )
}